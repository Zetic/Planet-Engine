name: Apply worldgen crash report integration

on:
  push:
    branches: [feat/worldgen-crash-report]

permissions:
  contents: write

jobs:
  apply:
    if: ${{ contains(github.event.head_commit.message, '[crash-report-driver]') }}
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@v4
        with:
          ref: feat/worldgen-crash-report
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
      - name: Apply integration patch
        run: python3 scripts/apply-worldgen-crash-report.py
      - name: Install dependencies
        run: npm ci
      - name: Build browser modules
        run: npm run build
      - name: Run TypeScript regressions
        run: npm run test:ts
      - name: Commit integration
        run: |
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add src index.html styles tests dist
          git diff --cached --quiet && exit 0
          git commit -m 'feat(worldgen): add crash report diagnostics [crash-report-applied]'
          git push origin HEAD:feat/worldgen-crash-report
